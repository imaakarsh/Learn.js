const name = "aakarsh-rk"
const repoCount = 34;

console.log(`hello my name is ${name} and my repo is ${repoCount}`);
console.log(name.toUpperCase());
console.log(name.charAt(2));

const game = "Aakarsh"
const gameName = game.substring(0,4) 
console.log();


const 